/**
 * Game loop tick interval in milliseconds.
 */
export const TICK_INTERVAL = 100;

/**
 * Maximum delta time in milliseconds to prevent huge updates after background tab throttling.
 */
export const MAX_DELTA_MS = 200;
