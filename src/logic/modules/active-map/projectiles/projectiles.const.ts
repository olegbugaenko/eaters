// Ring trail limits to prevent performance degradation
export const MAX_RINGS = 512; // Maximum rings across all projectiles
export const MAX_RINGS_PER_FRAME = 16; // Maximum rings spawned per frame (across all projectiles)

export const MAX_PROJECTILE_STEPS_PER_TICK = 5;
export const MIN_MOVEMENT_STEP = 2;
export const OUT_OF_BOUNDS_MARGIN = 50;
