{
  "name": "eaters-react-electron",
  "version": "1.0.0",
  "description": "React + TypeScript application with Electron support",
  "author": "Eaters Team",
  "main": "electron/main.js",
  "private": true,
  "scripts": {
    "start": "webpack serve --mode development",
    "start:demo": "node -e \"process.env.IS_DEMO='1'; const { spawnSync } = require('child_process'); const result = spawnSync('webpack', ['serve', '--mode', 'development'], { stdio: 'inherit', shell: true, env: process.env }); process.exit(result.status ?? 0);\"",
    "build": "webpack --mode production",
    "build:demo": "node -e \"process.env.IS_DEMO='1'; const { spawnSync } = require('child_process'); const result = spawnSync('webpack', ['--mode', 'production'], { stdio: 'inherit', shell: true, env: process.env }); process.exit(result.status ?? 0);\"",
    "build:electron": "npm run build && electron-builder --config electron-builder.config.js --win",
    "build:electron:demo": "node -e \"process.env.IS_DEMO='1'; const { spawnSync } = require('child_process'); const result = spawnSync('npm', ['run', 'build:electron'], { stdio: 'inherit', shell: true, env: process.env }); process.exit(result.status ?? 0);\"",
    "electron": "node scripts/run-electron-dev.js",
    "electron:dev": "concurrently \"npm start\" \"wait-on http://localhost:3000 && node scripts/run-electron-dev.js\"",
    "dist": "npm run build:electron",
    "pack": "electron-builder --config electron-builder.config.js --dir",
    "test": "tsc -p tsconfig.tests.json && node build/tests/tests/run.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-joyride": "^2.9.3"
  },
  "devDependencies": {
    "@types/react": "^18.2.15",
    "@types/react-dom": "^18.2.7",
    "@typescript-eslint/eslint-plugin": "^6.2.0",
    "@typescript-eslint/parser": "^6.2.0",
    "concurrently": "^8.2.0",
    "css-loader": "^6.8.1",
    "electron": "^26.1.0",
    "electron-builder": "^24.6.3",
    "eslint": "^8.45.0",
    "eslint-plugin-react": "^7.33.2",
    "eslint-plugin-react-hooks": "^4.6.0",
    "html-webpack-plugin": "^5.5.3",
    "style-loader": "^3.3.3",
    "ts-loader": "^9.4.4",
    "typescript": "^5.1.6",
    "wait-on": "^7.2.0",
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
